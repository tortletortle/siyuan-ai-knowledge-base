# 显示 ↔ 知识 提取契约 v1

> 笔记随便长，提取不受惊。显示层和知识层之间，只通过本契约说话。

## 1. 一句话

- **正文归人**：措辞、排版、标题叫什么，显示层完全自由。
- **纸条（IAL 块属性）归机器**：提取器只认纸条和本契约列出的标题，不猜、不读心。
- **回写只碰纸条**：机器永远不改正文，只写 `custom-kb-*` 属性。

## 2. 提取器保证（`src/block-index.mjs`）

| 优先级 | 规则 | 说明 |
|---|---|---|
| 0 | 非内容块（`d/l/root`）和空块永远丢弃 | 纸条也救不回来 |
| 1 | 自身贴了 `custom-kb-exclude="true"` → 丢弃 | 高于一切：宁缺毋滥 |
| 2 | 自身贴了 `custom-kb-include="true"` → 保留 | 能把排除区里的金子救回来 |
| 3 | 标题是纯 `卡片` / `测验`（允许 `#` 前缀和空白）→ 丢弃 | 兜底启发式，纸条优先于它 |
| 4 | 祖先命中第 1/3 条 → 丢弃 | 整区排除，子孙连坐 |
| 5 | 其余一律保留 | |

`custom-kb-topic="..."`：覆盖该块主题，不改变正文。
`custom-kb-status="active"`：回写挂牌“已入库 ✓”，只读展示用，不参与过滤。

## 3. 笔记侧约定（人要做的，只有两件）

1. 新课从 `templates/lesson-template.md` 建文档，知识区/排除区骨架已经摆好；
2. 两种情况贴纸条（右键块标 → 属性，或批量脚本）：
   - 排除区里有金子 → 贴 `custom-kb-include="true"`；
   - 知识区里有闲聊/通知 → 贴 `custom-kb-exclude="true"`。

除此之外，写作时脑子里不需要有知识库。

## 4. 回归保证

`test/contract.test.mjs` 锁定本契约：同一份知识，三种显示（朴素 / 花哨样式 /
标题变体 + 纸条），提取结果必须完全一致。显示改崩了，测试先报警。

## 5. 原生功能映射（最适配思源的用法）

| 我们的需求 | 思源原生能力 | 用法 |
|---|---|---|
| 课程骨架 | 文档模板（`data/templates/*.md`，`/` 触发） | 直接用 `templates/lesson-template.md`，纸条行调用时自动写成块属性 |
| 纸条读写 | 自定义属性（块标菜单 → 属性，`custom-` 前缀）+ API `/api/attr/setBlockAttrs` | 手工贴用菜单，批量贴/回写挂牌走 API，只碰属性不碰正文 |
| 术语别名 | 原生别名（块标菜单 → 命名/别名，IAL `alias`） | 逗号分隔写多个，提取器直读并喂给检索加权，替代手工 `aliases.json` |
| 已入库清单 | SQL 嵌入块 `{{SELECT ...}}` | 模板自带“本课已入库”活查区，入库后自动刷新，无需重新生成 |
| 卡片复习 | 原生闪卡（块标菜单 → 闪卡，FSRS 算法） | 制卡和入库正交：知识点可制卡，问答对卡片区整区 `custom-kb-exclude` |
| 图谱点亮 | 原生关系图谱（`Alt+8` / 全局 `Alt+9`）+ 反链面板 | MOC 文档的块引点亮图谱；写完课后去“提及”面板把真关联一键转块引，边自然生长 |
| 已入库徽标 | 外观 CSS 代码片段 | 粘贴 `snippets/kb-badges.css`，删掉即恢复原样 |

## 6. 版本历史

- v1（2026-09-10）：IAL 纸条纳入/排除、主题覆盖、回写挂牌字段、原生别名直读。
