# 显示 ↔ 知识 提取契约 v1

> 笔记随便长，提取不受惊。显示层和知识层之间，只通过本契约说话。

## 1. 一句话

- **正文归人**：措辞、排版、标题叫什么，显示层完全自由。
- **纸条（IAL 块属性）归机器**：提取器只认纸条和本契约列出的标题，不猜、不读心。
- **回写只碰纸条**：机器永远不改正文，只写 `custom-kb-*` 属性。

## 2. 提取器保证（`src/block-index.mjs`）

| 优先级 | 规则 | 说明 |
|---|---|---|
| 0 | 非内容块（`d/l/root`）和空块永远丢弃 | 纸条也救不回来 |
| 1 | 自身贴了 `custom-kb-exclude="true"` → 丢弃 | 高于一切：宁缺毋滥 |
| 2 | 自身贴了 `custom-kb-include="true"` → 保留 | 能把排除区里的金子救回来 |
| 3 | 标题是纯 `卡片` / `测验`（允许 `#` 前缀和空白）→ 丢弃 | 兜底启发式，纸条优先于它 |
| 4 | 祖先命中第 1/3 条 → 丢弃 | 整区排除，子孙连坐 |
| 5 | 其余一律保留 | |

`custom-kb-topic="..."`：覆盖该块主题，不改变正文。
`custom-kb-status="active"`：回写挂牌“已入库 ✓”，只读展示用，不参与过滤。

## 3. 笔记侧约定（人要做的，只有两件）

1. 新课从 `templates/lesson-template.md` 建文档，知识区/排除区骨架已经摆好；
2. 两种情况贴纸条（右键块标 → 属性，或批量脚本）：
   - 排除区里有金子 → 贴 `custom-kb-include="true"`；
   - 知识区里有闲聊/通知 → 贴 `custom-kb-exclude="true"`。

除此之外，写作时脑子里不需要有知识库。

## 4. 回归保证

`test/contract.test.mjs` 锁定本契约：同一份知识，三种显示（朴素 / 花哨样式 /
标题变体 + 纸条），提取结果必须完全一致。显示改崩了，测试先报警。

## 5. 版本历史

- v1（2026-09-10）：IAL 纸条纳入/排除、主题覆盖、回写挂牌字段。
