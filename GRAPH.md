# 关系图谱

> 目标：在笔记里形成完整的关系图，且原文零污染。

## 1. 图的三层边

| 层 | 边 | 谁来定 | 状态 |
|---|---|---|---|
| 引用边 | A 引用了 B（`references`） | 机器全自动：解析 `((id))` / `siyuan://blocks/id` / 块引 HTML | ✅ 已实现 |
| 结构边 | 同文档、同知识区 | 机器全自动：`doc_id` / `kb_topic` 分组 | ✅ 已实现（MOC 按文档分组） |
| 语义边 | 前置 / 对比 / 应用 | 人在审核时点，或以后上模型判断 | 🚧 未实现：机器不猜语义 |

“完整”的诚实定义：引用边 + 结构边全自动完整；语义边永远需要人（或模型）确认。

## 2. 在哪里看图

### 路径 A：思源原生关系图谱（推荐，零污染）

```bash
npm run graph -- <index.json> <out-dir>   # 得到 moc.md
```

把 `moc.md` 粘贴进思源一个新文档（叫“知识地图”），原生图谱即被点亮。
原理：MOC 文档只含 `((块引))`，边都长在 MOC 上，原文一个字都不用改。

局限：原生图谱显示的是“块引关系”，MOC 法形成以地图为中心的星型；
知识点之间的直接语义边请看路径 B。

### 路径 B：外部可视化（Mermaid）

同一命令还会生成 `graph.md`（Mermaid `flowchart`）和 `graph.json`
（节点/边/未解析引用，可接 D3、ECharts 或其他图工具）。

## 3. 不做的事

- 不把块引写回原文段落：那会污染显示，违反 `KB_CONTRACT.md` 回写铁律。
- 不从正文猜语义边：`categoryFor` 式的正则猜测已证明不可靠；
  语义边只接受人工标注（`review-state.json` 的 `related` 字段即候选来源）。
- 不静默丢弃：引用目标不在索引中时，进 `unresolved` 清单，不编造边。
