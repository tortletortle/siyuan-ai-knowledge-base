name: test
on:
  push:
    branches: [main, arena/01a08b72-siyuan-ai-knowledge-base]
  pull_request:
jobs:
  node:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm test
      - run: npm run eval:quality
