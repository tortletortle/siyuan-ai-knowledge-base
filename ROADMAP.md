# 总计划：距离终点有多远

> 一句话版本：按任务数约完成 **40%**，按风险约排除 **70%**——
> 地基和最难的解耦问题已解决，剩下的是验证 + 既定任务，没有未知生死题。

## 0. 终点长什么样（Done 的定义）

终点不是“完美系统”，而是这 8 条全部成立：

1. 写笔记零负担：L0 不用想格式，L1 两分钟贴完纸条。
2. 一篇新课 7 分钟走完 L0→L2，排版零破坏。
3. 已入库的块自动挂绿牌 `[✓ 已入库]`，不用手贴。
4. 笔记改了，对应知识自动变黄牌待复核，不静默撒谎。
5. 思源里有活的知识地图 + 关系图谱星型可看。
6. 检索可信：私有集 Hit@3≥0.85、污染率=0、无答案误命中≤0.1。
7. 第二批审核政策跑通（含驳回，不强制全员 active）。
8. 每次 push 自动跑测试 + fixture 评测（CI 已有，保持不破）。

## 1. 你现在在哪（M0 已完成 ✅）

- 设计总纲（`DESIGN.md`）+ 契约（`KB_CONTRACT.md`）+ 图谱（`GRAPH.md`）。
- 读链路全通：导出→索引→检索→评测→图谱，schema 无断裂。
- 契约 v1：纸条过滤/主题/原生别名/回写字段，`test/contract.test.mjs` 锁死。
- 图谱三件套：`graph.json` / Mermaid / MOC，`npm run graph` 可跑。
- 真模板 + CSS 徽标 + 样例（`templates/`、`snippets/`、`examples/`）。
- 审计修复：指标分母、错标注、CI、`.gitignore` 加固。
- 状态：`npm test` 34/34，fixture 基线全绿。

## 2. M1 能用（预计 1–2 天，负责人：你）

目标：亲眼看到绿牌 + 地图 + 图谱，亲手验证两个核心假设。

| # | 任务 | 耗时 | 验收 |
|---|---|---|---|
| V1 | 模板拷入 `data/templates/`，`/` 调用 | 10 分钟 | 骨架出来；**记录纸条行是否自动生效** |
| V2 | 右键块 → 属性，手加 `custom-kb-exclude=true` | 5 分钟 | **记录 UI 是否接受连字符属性名** |
| V3 | 导出快照 → 建索引，确认 V2 的块消失、显示未变 | 15 分钟 | 索引 diff 为空、笔记无变化 |
| V4 | 原生别名检索验证（`FSM` 命中中文知识） | 10 分钟 | `matches` 含 `alias` |
| V5 | 粘贴 CSS 片段，手挂 3 个绿牌 | 10 分钟 | 绿牌出现；删 CSS 即消失 |
| V6 | 真实快照跑索引 + graph（即 T6） | 30 分钟 | 只记聚合数字：块数/边数/未解析数/孤点率 |
| V7 | `moc.md` 粘进“知识地图”，`Alt+8` 看图谱 | 10 分钟 | 星型出现，块引可点 |
| V8 | SQL 嵌入块活查已入库清单 | 10 分钟 | 自动列出，改动后刷新 |

M1 完成标志：V1、V2 有结论（无论行不行，都有 fallback），V6 数字已记录。

## 3. M2 闭环（预计 2–3 天，负责人：本地 AI，按 DESIGN.md §10）

目标：写链路打通，一篇新课 7 分钟入库。

| # | 任务 | 耗时 | 验收（见 DESIGN.md） |
|---|---|---|---|
| T1 | 回写挂牌脚本（`setBlockAttrs`，`--dry-run` 默认开，幂等） | 半天 | mock 测试断言请求体形状 |
| T2 | 批量打标脚本（只读给建议） | 半天 | fixture 输出 3 类建议 |
| T3 | 过期检测脚本（hash 对比 → `stale`） | 2 小时 | 改前/改后 fixture 恰好标出改过的 |
| B2 | 第二批审核政策（允许驳回，替代“强制全员 active”） | 2 小时 | 含 1 驳回的 review-state 能跑通 |

M2 完成标志：在真实一篇课上计时走完 L0→L2 ≤7 分钟，绿牌自动出现。

## 4. M3 可信（预计 3–5 天，本地 AI + 你配合）

目标：用数字证明检索可信。

| # | 任务 | 耗时 | 验收 |
|---|---|---|---|
| E1 | 建私有评测集（约 39 条，见 `PRIVATE_K17_DATASET.md`），**冻结 `annotation_version`** | 1–2 天（人工） | 三文件放仓库外，齐 T7 分组字段 |
| T7 | 分组指标（意图/标签/课程分组） | 半天 | 报告出现分组表 + 测试 |
| B0 | 当前算法跑基线并存档（仓库外） | 1 小时 | 基线数字 + 日期，不改代码 |
| T4 | `Intl.Segmenter` 分词替换 bigram | 1 天 | 旧测试全绿 + 新旧基线对比；倒退则保留双实现 |
| T5 | 语义边标注链路（`related` → `relations` → 图谱 typed 边） | 半天 | 端到端测试出现 typed 边 |
| G1 | 门槛判定：Hit@3≥0.85、污染=0、误命中≤0.1 | — | 达标→终点；不达标→看 §6 风险 R3 预案 |

M3 完成标志：8 条 Done 定义（§0）全成立。**到这里就是终点。**

## 5. M4 可选（终点之后，不排期）

embedding 混合检索、LLM 辅助语义边、属性视图审核看板、多课程扩展、
`trust`/`approximateTokens` 精化。想做再做，不做系统照样转。

## 6. 风险与预案

| # | 风险 | 概率 | 预案 |
|---|---|---|---|
| R1 | 模板纸条行不自动生效（V1） | 中 | 手贴 10 秒 + T2 脚本批量打；设计不变 |
| R2 | UI 拒绝连字符属性名（V2） | 中 | 打标全走脚本/API；设计不变 |
| R3 | 真实数据 Hit@3 不达标 | 中 | T4 分词 → 仍不行则 BM25（T4 已预留接口位）；架子保证数字先行 |
| R4 | 私有评测集标注漂移 | 低 | E1 冻结版本号；改标注 = 新版本 + 重跑基线 |

## 7. 进度条（打勾用）

```text
M0 设计+读链路  ████████████████████ 100% ✅
M1 本地验证     ░░░░░░░░░░░░░░░░░░░░   0% ← 你在这里，从 V1 开始
M2 写链路闭环   ░░░░░░░░░░░░░░░░░░░░   0% ← 本地 AI，T1 起手
M3 数字可信     ░░░░░░░░░░░░░░░░░░░░   0%
终点（M3 完成） 预计 1–2 周（按业余时间折算）
```

总耗时估算（纯投入）：你约 1 天（验证 + 标注），本地 AI 约 4–6 天当量。
日历时间取决于你的空闲，不取决于技术难度——**没有需要攻关的未知题了**。
